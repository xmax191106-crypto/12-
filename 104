total = 0

while True:
    price = input("Введите цену или 0 для выхода: ")
    if price == "0":
        break
    price = int(price)
    if price < 0:
        print("Ошибка цены")
        continue
    total += price

if total >= 1000:
    total = total * 0.9

print("К оплате: ", total)
